import { NavLink } from "react-router-dom";

export default function Menu() {
  return (
    <nav>
      <NavLink>Dashboard</NavLink>
      <NavLink>Dashboard</NavLink>
      <NavLink>Dashboard</NavLink>
      <NavLink>Dashboard</NavLink>
      <NavLink>Dashboard</NavLink>
    </nav>
  );
}
